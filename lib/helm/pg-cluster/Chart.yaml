apiVersion: v2
name: pg-cluster
description: Shared wrapper chart that renders the CloudNativePG resources for one Postgres instance
type: application
version: 0.1.0 # local chart, never bump version
