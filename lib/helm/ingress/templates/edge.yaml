{{- /* Auto-emits this consumer's edge from its `ingresses:` values, so a consumer needs no template of its
       own. Guarded so a consumer that only wants the named-template helpers (google-sso calls
       ingress.renderIngress itself) gets no stray empty render. */ -}}
{{- if .Values.ingresses }}
{{ include "ingress.render" . }}
{{- end }}
