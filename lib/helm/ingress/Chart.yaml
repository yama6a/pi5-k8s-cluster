apiVersion: v2
name: ingress
description: Shared chart that renders the ingress edge for a consumer's ingresses[], per host a Gateway listener + HTTPRoute + ReferenceGrant, and one multi-SAN Certificate per ingress. Renders no SSO.
type: application
version: 0.1.0 # local chart, never bump version
