apiVersion: v2
name: rabbitmq-topology
description: Shared chart that renders a workload's messaging topology against the one shared broker, exactly one User plus the Exchanges/Queues/Bindings it owns and one aggregated Permission.
type: application
version: 0.1.0 # local chart, never bump version
