# The CronJob's identity — bound to the cluster-wide read-only ClusterRole on Redis CRs (rbac.yaml) so the
# `list` container can `kubectl get redis -A`.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: redis-backup
  namespace: {{ .Release.Namespace }}
