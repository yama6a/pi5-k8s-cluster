apiVersion: v2
name: victoria-metrics-operator-crds
description: VictoriaMetrics operator CRDs only, no controller. Split from the operator chart so the large CRDs sync ServerSideApply.
type: application
version: 0.1.0 # local chart, never bump version

dependencies:
  - name: victoria-metrics-operator-crds
    version: "0.14.0" # must match 01_victoria_metrics_operator; renovate groups them, bump together
    repository: https://victoriametrics.github.io/helm-charts/
