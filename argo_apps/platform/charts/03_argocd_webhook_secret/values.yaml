# Nothing to configure. The SealedSecret in templates/ is written verbatim by 08_argocd_webhook.sh
# (it seals a generated secret against the live controller's key). File kept for convention (every chart
# has one).
