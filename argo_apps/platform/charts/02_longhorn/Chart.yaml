apiVersion: v2
name: longhorn
description: Longhorn, the cluster's distributed block storage and default StorageClass, on the XFS 'longhorn' volume 03d mounts.
type: application
version: 0.1.0 # local chart, never bump version

dependencies:
  - name: longhorn
    version: "1.12.0"
    repository: https://charts.longhorn.io
