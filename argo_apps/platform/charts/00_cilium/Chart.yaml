apiVersion: v2
name: cilium
description: Cilium, the CNI plus LB-IPAM/L2 LoadBalancer and WireGuard encryption. Bootstrapped by 04_cilium.sh, then adopted by ArgoCD.
type: application
version: 0.1.0 # local chart, never bump version

dependencies:
  - name: cilium
    version: "1.19.6"
    repository: https://helm.cilium.io
