apiVersion: v2
name: argocd
description: ArgoCD, the GitOps engine. Bootstrapped once by 05_argocd.sh, then self-managed from this chart.
type: application
version: 0.1.0 # local chart, never bump version

dependencies:
  - name: argo-cd
    version: "10.2.1"
    repository: https://argoproj.github.io/argo-helm
