apiVersion: v2
name: dead-node-watcher
description: Taints a node out-of-service once it has been NotReady long enough, so its volumes detach in seconds instead of minutes.
type: application
version: 0.1.0 # local chart, never bump version
