apiVersion: v2
name: priorityclass
description: The two shared PriorityClasses that order eviction under memory pressure, platform-critical and data-critical.
# The built-in system-cluster-critical / system-node-critical are admission-restricted to kube-system, so
# components in their own namespaces cannot use them.
type: application
version: 0.1.0 # local chart, never bump version
