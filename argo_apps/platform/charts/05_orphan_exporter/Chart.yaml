apiVersion: v2
name: orphan-exporter
description: Flags deletionProtection-carrying stateful CRs (CNPG Clusters, OpsTree Redis, VM/VL stores) orphaned by a GitOps prune (owning Argo app gone) or lacking Argo tracking metadata.
type: application
version: 0.1.0                 # local chart, never bump version
appVersion: "1.0"
