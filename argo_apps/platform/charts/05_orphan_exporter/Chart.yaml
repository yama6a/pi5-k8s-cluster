apiVersion: v2
name: orphan-exporter
description: Flags deletionProtection CRs orphaned by a GitOps prune, and exports per-CNPG-server backup recoverability from the barman ObjectStore status.
# Name is stale (it does the backup gauges too). Left alone because renaming a chart orphans its
# release-named resources.
type: application
version: 0.1.0 # local chart, never bump version
appVersion: "1.0"
