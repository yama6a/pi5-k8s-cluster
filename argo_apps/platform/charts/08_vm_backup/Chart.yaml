apiVersion: v2
name: vm-backup
description: One CronJob that exports the VMSingle and VLSingle stores over HTTP and streams each gzip'd to S3. Sealed S3 creds committed by lib/shell/17_vm_backup.sh.
type: application
version: 0.1.0 # local chart, never bump version
