{{/*
The whole platform ingress is rendered by the shared ingress library from this chart's values. This
one line is deliberately the entire template: no edge YAML is hand-written here (or in any consumer), it
all lives in lib/helm/ingress/templates. See 07_ingress.md.
*/}}
{{ include "ingress.render" . }}
