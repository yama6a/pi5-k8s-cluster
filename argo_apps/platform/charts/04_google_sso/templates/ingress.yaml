{{/*
The shared OIDC callback hosts (google-sso.<domain>) are rendered by the ingress-edge library from
`ingressEdge.callbackDomains` in the library values — one per domain, each an SSO ingress in front of the
shared whoami (templates/callbacks.yaml) whose redirectURL derives to itself. That list is THE single source
for which domains have SSO; every app ingress's `domain` is guarded against it. One line by design; no
edge YAML hand-written. See 07_ingress.md ("Adding an SSO domain").
*/}}
{{ include "ingress-edge.callbacks" . }}
