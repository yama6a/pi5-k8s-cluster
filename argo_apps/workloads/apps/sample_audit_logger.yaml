# sample-audit-logger: the sample-app /auditor binary. A messaging-only sink on the user-audit-logger fanout.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sample-audit-logger
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=true # else server-defaulted topology CR fields read as perpetual OutOfSync
spec:
  project: default
  revisionHistoryLimit: 0
  source:
    repoURL: https://github.com/yama6a/pi5-k8s-cluster
    targetRevision: HEAD
    path: argo_apps/workloads/charts/sample_audit_logger
  destination:
    server: https://kubernetes.default.svc
    namespace: sample-audit-logger
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: -1
      refresh: true
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
