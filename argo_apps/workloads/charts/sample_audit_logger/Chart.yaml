apiVersion: v2
name: sample-audit-logger
description: The sample-app /auditor binary. A pure sink on the user-audit-logger fanout, no HTTP, no Postgres, no ingress.
type: application
version: 0.1.0 # local chart, never bump version

dependencies:
  - name: rabbitmq-topology
    version: "*"
    repository: "file://../../../../lib/helm/rabbitmq-topology"
