{{/*
This worker's whole RabbitMQ topology (its user + its private event queue/binding + one aggregated
permission) is rendered by the shared rabbitmq-topology library from the `rabbitmq-topology:` block in
values.yaml. One line by design: no topology YAML is hand-written here. See 11_messaging.md.
*/}}
{{ include "rabbitmq-topology.render" . }}
